First, ensure that the RALA, RF Switch, and Horn controlling scripts are operational, and the horn is pointed in the direction the antenna controller expects it to be pointed in

To run the automated steering:
1) make run_tran
2) from automated_steering import *     # Within DragonRadio's Python interactive console
3) run_*_*(radio)          # i.e. run_simple_sweep(radio) or run_ml_connection(radio)  